# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Map static resources from the /public folder to the /assets URL path

GET   /insertCard/:card                       controllers.MemberController.insertCard(card: Card)

GET   /findMember/:card                       controllers.MemberController.insert

POST  /registerMember                         controllers.MemberController.registerMember

GET   /checkBalance/:card                     controllers.MemberController.checkFunds(card:Card)

POST  /topUpBalance/:card/:balance            controllers.MemberController.addFunds(card:Card, balance:Int)

POST  /transaction/:card/:costOfGoods         controllers.MemberController.transaction(card:Card, costOfGoods:Int)
