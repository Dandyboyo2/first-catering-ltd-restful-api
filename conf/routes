# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Map static resources from the /public folder to the /assets URL path

GET   /insertCard/:card                       controllers.EmployeeController.insertCard(card: Card)

GET   /findEmployee/:card                       controllers.EmployeeController.findEmployeeByID(card: Card)

POST  /registerEmployee                         controllers.EmployeeController.registerEmployee

GET   /checkBalance/:card                     controllers.EmployeeController.checkBalance(card:Card)

POST  /topUpBalance/:card/:balance            controllers.EmployeeController.topUpBalance(card:Card, balance:Int)

POST  /accountTransaction/:card/:costOfGoods         controllers.EmployeeController.accountTransaction(card:Card, costOfGoods:Int)
